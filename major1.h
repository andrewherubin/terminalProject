#ifndef MAJOR1_H
#define MAJOR1_H
#include <stdio.h>
#include <stdlib.h>
//This file contains all the function prototypes for this project, as well as all include statements for each function.

//Cody's function prototype
void clz(unsigned long long int p); // clz function takes the input integer only

//Aracely's function prototype
void endian(unsigned long int p); //add in whatever arguments are needed. e.g. (int x, int y);

//Walter's function prototype
unsigned int rotate(unsigned int num, unsigned int k); //add in whatever arguments are needed. e.g. (int x, int y);

//Andrew's function prototype
int parity(unsigned long int p); //add in whatever arguments are needed. e.g. (int x, int y);

#endif